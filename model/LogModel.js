export default class logModel {
    constructor(logCode,logDetails,observesImage,logDate) {
        this.logCode = logCode;
            this.logDetails = logDetails;
        this.observesImage = observesImage;
        this.logDate = logDate;




    }

}